<%= form_for(@schedule_drugs_conflict) do |f| %>
  <% if @schedule_drugs_conflict.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule_drugs_conflict.errors.count, "error") %> prohibited this schedule_drugs_conflict from being saved:</h2>

      <ul>
      <% @schedule_drugs_conflict.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :schedule_drug1_id %><br />
    <%= f.number_field :schedule_drug1_id %>
  </div>
  <div class="field">
    <%= f.label :schedule_drug2_id %><br />
    <%= f.number_field :schedule_drug2_id %>
  </div>
  <div class="field">
    <%= f.label :reason %><br />
    <%= f.text_field :reason %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
