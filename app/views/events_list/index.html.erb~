<%= render 'sidenav' %>

<div class = "page_content">

<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h1 class="section_title">Events List</h1>

<ul class="events_list">
<% @events.each do |event| %>
  <li>
    <ul class="object_item">
      <li><h3><%= event.name %></h3></li>
      <li><%= link_to 'edit', edit_event_path(event.id), {:method => :get} %></li>
      <li><%= link_to 'delete', event, confirm: 'Are you sure?', method: :delete %></li>
    </ul>
  </li>  
<% end %>
</ul>

</div>
