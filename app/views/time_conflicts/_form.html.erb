<%= form_for(@time_conflict) do |f| %>
  <% if @time_conflict.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_conflict.errors.count, "error") %> prohibited this time_conflict from being saved:</h2>

      <ul>
      <% @time_conflict.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :schedule_id %><br />
    <%= f.number_field :schedule_id %>
  </div>
  <div class="field">
    <%= f.label :is_dismissed %><br />
    <%= f.check_box :is_dismissed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
