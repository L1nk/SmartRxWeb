<% if user_signed_in? %>
  <% has_notifications = session[:has_notifications] %>
  <% if has_notifications? %>
    <li>
      <%= link_to('!!!', '#', :class => 'top_link') %>        
    </li>
  <% else %>
    <li>
      <%= link_to('!', '#', :class => 'top_link') %>        
    </li>
  <% end %>
<% end %>
